# lightning_trade 和 BADelay500W 合并

## 需求背景与目标

- 当前 `lightning_trade` 已有 `server/argus_single`，但该目录现有实现不再需要，应由同级项目 `../BADelay500W/argus_single` 完整替换；`lightning_trade` 的其他服务端和客户端模块继续保留。
- `BADelay500W/argus_single` 的账户、交易、风控、监控币种、Telegram 等参数主要维护在 `configs/application.properties`，会话凭证维护在 `configs/session.json`，修改依赖人工编辑服务器文件。
- 当前项目已有 MySQL/GORM、Redis、Go Manager API 和 Next.js 管理端。目标是将 Argus 配置结构化保存到数据库，通过管理端维护和发布，使运行中的 Argus 可安全热加载。**数据库是配置的唯一来源**，Redis 只承担控制/通知信道与心跳，不缓存配置快照（决策见 §决策记录：配置不经 Redis 缓存）。
- `session.json` 中 Cookie、Token、Sentry 等运行态会话数据也需要入库，并支持 Argus 自动刷新回写。
- 管理端除配置维护外，还需要展示 Argus 在线状态、当前配置版本，并提供启动、停止、重启和热加载操作。

## 范围与不做的事

### 本次范围

1. 删除 `server/argus_single` 当前业务实现，以 `../BADelay500W/argus_single` 完整替换，并适配当前仓库公共模块和构建方式。
2. 仅迁移 `../BADelay500W/argus_single/configs/application.properties` 这一份主配置，不迁移 `application_1.properties`、`application_2.properties` 和 `dev.properties`。
3. 建立单实例 Argus 配置、交易账户、账户风控、监控币种、全局参数、通知配置和运行态会话的数据库模型。
4. 在 `server/service`、`server/manager-api` 增加配置维护、校验、发布和运行控制接口。
5. 改造 Argus 启动和运行链路：启动时从数据库加载生效配置；运行中以「定时比对内容指纹」为准据检测变更并原子热加载，Redis 通知只作为提前触发一次比对的加速信号。
6. 将 Cookie、Token、Sentry 等会话信息从文件迁入数据库，并允许 Argus 更新后安全回写。
7. 在 `client/manager` 增加 Argus 配置、运行状态和启动/停止/重启/热加载页面。
8. 导入现有 `application.properties`、`session.json` 并完成端到端回归。

### 暂不纳入

- 不保留 `lightning_trade/server/argus_single` 当前独有的 AI 开仓、AI 平仓和 BTC 市场数据实现。
- 不迁移 `../BADelay500W/argus_single_AI`。
- 不迁移或维护 `application_1.properties`、`application_2.properties`、`dev.properties`，首版只支持一套 Argus 实例配置。
- 不在本轮实施代码、数据库或任务面板写入；本轮只维护需求大纲和拆解预览。
- 不把数据库连接、Redis 连接等读取远端配置所必需的引导参数存入数据库或 Redis。
- 暂不设计多机器、多实例或集群编排；运行控制任务先按 Manager API 与 Argus 同机、调用受限启停脚本的默认方案拆解，后续实施时可根据实际部署拓扑调整实现。

## 关键约束

- `server/argus_single` 采用完整替换，不进行旧 Argus 业务代码合并；`server/common`、`server/service`、`server/manager-api`、`client/manager` 等其他项目模块不得因替换被删除。
- BADelay 源码中的 `badelay500w/common` 模块路径必须适配为当前仓库的 `common`，并统一 Go 版本、依赖和构建脚本。
- MySQL 是配置与会话的**唯一**事实来源，读写都直接落库。Redis 只保存心跳与控制/通知消息，**不保存配置快照，也不保存版本号**——配置读取一天仅数次（启动与重载），缓存省下的那点延迟换不回「缓存与库不一致」这种静默失效（决策见 §决策记录）。
- 配置发布采用「数据库事务保存成功 → 发布变更通知（可选加速）」的顺序：事务是唯一的正确性边界，通知丢失不影响最终一致——实例的定时比对会在一个周期内自行发现。任一步失败不得覆盖上一有效版本。
- Argus 热加载必须先完整解析和校验新快照，再原子替换运行配置；失败时继续使用旧配置并上报错误，不能在交易中使用半套新旧配置。
- 交易账户包含 API Key、Secret、Passphrase、账号密码、Google Auth Key、Cookie、Token、Sentry 和 Telegram Token 等敏感信息。**这些字段只以明文存在于数据库**（决策见 §决策记录：取消应用层加密；配置已不进 Redis，见 §决策记录：配置不经 Redis 缓存），补偿手段是接口脱敏、日志过滤和页面遮罩三层必须完整，且数据库账号权限与网络可达范围要收紧。
- 运行态会话与静态账户配置分开建模，Argus 可回写会话，但不能通过普通列表接口返回完整值。
- Redis 现有入口位于 `server/common/middleware/redis/RedisClient.go`，当前只封装基础 KV，需要补充带 context 的 Set/Get、Publish/Subscribe、心跳 TTL 和错误返回能力。
- 启停脚本已有真实落点 `server/argus_single/script/start.sh`、`stop.sh`；进程控制必须使用受限命令映射，不能让管理端提交任意 shell 命令。
- 管理端 HTTP 请求必须使用 `client/manager/src/utils/axios.ts`，页面遵循 Next.js App Router、Ant Design 和 class DTO/API 分层。
- 当前任务面板没有可用里程碑和模块，因此预览中的里程碑、模块只能标记为“未选择（现有无选项）”。

## 勘察到的落点（真实模块/目录/接口）

### Argus 主程序

- 完整替换目标：`server/argus_single`
- 唯一迁移来源：`../BADelay500W/argus_single`
- 启动入口：`server/argus_single/cmd.go`
- 初始化链路：`server/argus_single/initialization/initialization.go`
- 配置映射：`server/argus_single/pkg/trade/config.go`、`account_params.go`
- 监控与风控：`server/argus_single/pkg/monitor/price_monitor.go`、`account_monitor.go`、`signal_scheduler.go`、`exit_evaluator.go`、`trail_persist.go`
- 会话读写：`server/argus_single/pkg/trade/session_store.go`、`deepcoin_login_service.go`
- 运行状态接口：`server/argus_single/pkg/health`、`routers`
- 启停脚本：`server/argus_single/script/start.sh`、`stop.sh`
- 导入来源：`../BADelay500W/argus_single/configs/application.properties`、`session.json`

### 后端配置与 Redis

- 数据库基础：`server/common/middleware/db/db.go`、`base.go`
- Redis 基础：`server/common/middleware/redis/RedisClient.go`
- 新服务建议：`server/service/argus_config`
- 新管理 API：`server/manager-api/pkg/argus_config`
- 路由注册：`server/manager-api/routers/register.go`
- 初始化入口：`server/manager-api/initialization/initialization.go`
- 现有服务结构参考：`server/service/coin_platform`、`coin_user`、`pressure`
- `CoinPlatformAccount` 字段不足以承载 Argus 登录态和账户级风控，Argus 账户使用独立模型，可按需要关联平台记录。

### 管理端

- 根目录：`client/manager`
- 导航入口：`client/manager/src/components/manager-shell/ManagerShell.tsx`
- 页面参考：`client/manager/src/app/(console)/trade-strategy`、`platform`、`coin-user`
- 新页面：`client/manager/src/app/(console)/argus-config`
- 新 API：`client/manager/src/app/(console)/argus-config/api/argus-config.api.ts`

## 配置与运行数据划分

1. **单实例基础配置**：服务端口、请求路径、日志目录、发布版本、启用状态。
2. **交易账户**：账户名、URL、API/登录凭证、持仓模式、方向、交易逻辑、下单数量和变体。
3. **账户风控**：止盈模式、移动止盈分层、风险预算、灾难止损、反向门禁和最大合约数。
4. **全局参数**：信号延迟、行情时效、仓位监控间隔和默认风险参数。
5. **监控币种**：symbol、DeepCoin 标识、交易标识、价差阈值、信号阈值和启用状态。
6. **通知配置**：Telegram Bot Token、Chat ID 和启用状态。
7. **运行态会话**：Cookie、Token、API Key、Sentry 信息、更新时间和有效状态。
8. **Redis 运行数据**：进程心跳、运行状态、控制命令及执行结果、配置变更通知。**不含配置快照与配置版本号**（决策见 §决策记录）。

## 任务拆解表

> 2026-08-20 已按最终确认方案成功写入 7 条新任务；没有复用已有任务。项目当前无里程碑和模块，任务负责人因需求未指定主负责人而保持未指派。

| 序号 | 任务键 | 任务标题 | 收益标签 | 负责人 | 里程碑 | 模块 | 类型 | 前置依赖 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | `t1-bc7efb6990` | 使用 BADelay500W 完整替换 argus_single | 主线统一、能力迁移、删除旧实现 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | capability | 无 | 已写入 |
| 2 | `t2-5e3c6d1de7` | 建立单实例 Argus 配置与会话数据库模型 | 配置入库、会话持久化、敏感数据治理 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | asset | 无 | 已写入 |
| 3 | `t3-8a078c6461` | 实现配置管理 API 与 Redis 发布通道 | 统一管理、安全发布、版本同步 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | capability | `t1-bc7efb6990`、`t2-5e3c6d1de7` | 已写入 |
| 4 | `t4-67a91e2935` | 实现 Argus 配置热加载与会话回写 | 无停机配置、原子切换、状态一致 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | capability | `t3-8a078c6461` | 已写入 |
| 5 | `t5-8816c71ad5` | 实现 Argus 心跳与安全运行控制 | 在线可见、启停控制、故障反馈 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | capability | `t4-67a91e2935` | 已写入 |
| 6 | `t6-a29ee957bb` | 新增管理端 Argus 配置与运行控制页面 | 可视化维护、热加载操作、状态可见 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | capability | `t5-8816c71ad5` | 已写入 |
| 7 | `t7-7e428fa033` | 导入主配置和会话并完成端到端验收 | 平滑迁移、风险收敛、可上线 | 未指派 | 未选择（现有无选项） | 未选择（现有无选项） | gap | `t6-a29ee957bb` | 已写入 |

## 任务详情与验收标准

### 任务 1：使用 BADelay500W 完整替换 argus_single

**目标**

删除当前 `server/argus_single` 业务实现，将 `../BADelay500W/argus_single` 作为唯一 Argus 主线迁入，其他 lightning_trade 模块保持不变。

**范围**

- 迁移 BADelay 的事件日志、健康检查、价格与账户监控、信号调度、平仓、移动止盈和风险控制。
- 不迁移二进制、PID、日志、运行数据及多余配置文件。
- 适配 `common` 模块路径、Go 版本、依赖和构建部署脚本。
- 删除当前 Argus 独有 AI 开平仓和 BTC 市场数据代码，不影响仓库其他服务。

**验收标准**

- `server/argus_single` 源码与 BADelay 主线功能一致并可独立编译。
- BADelay 已有单元测试可运行，初始化顺序覆盖路由、监控、交易、会话和通知。
- 不再残留旧 Argus AI 业务代码或 `badelay500w/*` 导入。
- `server/common`、`server/service`、`server/manager-api`、`client` 等目录不因替换丢失。

### 任务 2：建立单实例 Argus 配置与会话数据库模型

**目标**

将 `application.properties` 和 `session.json` 中需要维护的数据结构化保存到 MySQL。

**范围**

- 在 `server/service/argus_config/repository` 建立单实例配置、账户、账户风控、币种、通知和会话模型。
- 只支持一套主配置，可保留草稿版本与唯一已发布版本，不设计多个运行实例。
- 会话与静态账户分表，记录更新时间、有效状态和关联账户。
- 敏感字段明文存储，不做应用层加密（决策见 §决策记录）；仍需增加必要唯一索引和状态约束。

**验收标准**

- 模型可无损表达主 `application.properties` 和 `session.json` 的有效字段。
- 数据库只能存在一个当前已发布版本，但历史版本可追溯和回滚。
- Secret、Password、Google Auth Key、Cookie、Token、Sentry、Telegram Token 在库中按明文存储，可由运维直接用数据库客户端查改；对外则不得暴露——列表与详情接口一律返回 `******`（见 §决策记录）。
- GORM 迁移可重复执行且不破坏已有数据。

### 任务 3：实现配置管理 API 与发布通道

**目标**

提供数据库配置维护、完整校验与版本发布。发布只写数据库；变更通知走 Redis，但不是正确性依赖。

**范围**

- 新增 `server/service/argus_config` DTO、Repository、Service 和 `server/manager-api/pkg/argus_config` Handler。
- 支持配置、账户、风控、币种、通知和会话的维护与脱敏查询。
- 扩展 `server/common/middleware/redis`，增加显式错误返回、Publish/Subscribe 和心跳 TTL 能力。配置快照与版本键相关的读写能力已废弃（见 §决策记录）。
- 发布时事务保存数据库，随后发布变更通知；通知失败只记日志，不回滚已提交的发布。
- 敏感字段查询脱敏，留空更新不覆盖原值，发布动作记录操作者和版本。

**验收标准**

- 非法参数、缺少账户、无效币种或敏感字段处理失败时不能发布。
- Redis 写入或通知失败时，旧版本仍是唯一有效运行版本。
- API 不返回敏感明文（`maskSecret` 掩成 `******`）。敏感值只以明文存在于数据库（Argus 需要真值才能调交易所接口），依赖数据库账号权限与网络可达范围收紧；Redis 里不再有配置明文。
- 发布成功后可从数据库查询到新版本与快照摘要；实例在一个比对周期内（或收到通知后立即）切换到该版本。

### 任务 4：实现 Argus 配置热加载与会话回写

**目标**

让运行中的 Argus 自行发现数据库里的配置变更，在不中断进程的情况下安全切换配置，并将刷新后的会话写回数据库。

**范围**

- 启动时直接从数据库读取该实例的已发布版本并派生运行配置，不经任何缓存。
- 定时（默认 60 秒）从数据库重新派生运行配置，与当前生效配置比对**内容指纹**；不同则完成结构、关联与范围校验后原子切换。订阅配置变更频道只用于提前触发一次比对。
- 指纹必须对**派生后的运行配置**求取，不能对数据库原始行求取：原始行含 `updated_time` / `session_updated_at` 等审计列，而 Argus 自身的会话回写就会改动它们，对原始行求指纹会让进程每次回写会话都把自己热替换一遍。
- 采用原子配置引用或等效同步机制切换账户、风控、币种、Telegram 等运行参数。
- 明确不能安全动态替换的字段；此类字段发布后标记为需要重启，不强行热加载。
- 将登录刷新产生的 Cookie、Token、Sentry 等会话按账户回写数据库，并同步必要的 Redis 会话缓存。

**验收标准**

- 可在 Argus 运行中修改并发布可热加载字段，无需重启即可使用新版本。
- 新配置解析或校验失败时继续使用旧版本，并向状态接口记录失败版本和原因。
- 并发监控和交易读取配置时不存在半更新、空引用或数据竞争。
- 会话刷新后数据库记录更新，重启后可恢复使用，敏感值不进入普通日志。

### 任务 5：实现 Argus 心跳与安全运行控制

**目标**

让管理端可查看 Argus 在线状态、版本和错误，并安全执行启动、停止、重启及主动热加载。

**范围**

- Argus 周期性向 Redis 写入带 TTL 的心跳、PID、启动时间、当前版本、最后热加载结果和健康状态。
- Manager API 提供运行状态查询、启动、停止、重启和重新加载接口。
- 复用并加固 `server/argus_single/script/start.sh`、`stop.sh`，增加明确的 restart/control 适配层。
- 控制接口只允许固定动作，校验进程路径/PID，串行化控制请求并记录操作结果。
- 停止与重启优先优雅退出，给监控关闭和状态持久化留出时间。

**验收标准**

- 管理端可区分运行中、已停止、启动中、停止中、异常和心跳超时状态。
- 重复启动不会产生多个 Argus 进程，停止不会误杀其他进程。
- 启停、重启和热加载均返回可追踪结果，失败后状态与真实进程一致。
- 未授权用户不能调用控制接口，接口参数不能形成任意命令执行。

### 任务 6：新增管理端 Argus 配置与运行控制页面

**目标**

在 `client/manager` 中完成配置维护、发布、热加载和进程控制，替代文件及命令行操作。

**范围**

- 新增 `argus-config` 页面、API class、组件和 Hooks，并接入 `ManagerShell` 导航。
- 页面覆盖账户、风控、监控币种、通知、会话状态、配置版本与发布记录。
- 顶部展示在线状态、当前版本、启动时间和最后热加载结果。
- 提供发布并热加载、重新加载、启动、停止、重启按钮；停止和重启需二次确认。
- 敏感字段默认遮罩，只允许输入新值覆盖，不回显完整历史值。

**验收标准**

- 用户无需编辑服务器文件即可完成主配置和会话相关维护。
- 发布后可看到目标版本、热加载结果和当前运行版本是否一致。
- 进程按钮根据实时状态正确禁用，重复点击不会并发执行相同控制动作。
- 所有请求使用 `@utils/axios`，页面数据和表单具有严格 TypeScript 类型。

### 任务 7：导入主配置和会话并完成端到端验收

**目标**

将 BADelay 主 `application.properties` 和 `session.json` 安全迁入数据库，验证配置管理、Redis 热加载和运行控制链路可上线。

**范围**

- 提供一次性导入工具或受控导入流程，只读取主 `application.properties` 和 `session.json`。
- 导入前校验字段映射，敏感值不输出到控制台、日志和验收报告。
- 验证数据库发布、Argus 按指纹比对热加载（含通知丢失时靠定时比对兜住）、会话回写、心跳、启动、停止和重启。
- 回归多账户、监控信号、交易方向、仓位上限、止盈止损、Telegram、事件日志和健康接口。

**验收标准**

- 导入可重复演练，不重复创建当前生效配置，也不读取其他 properties 文件。
- 导入后字段与源文件逐项一致。敏感字段在库与 Redis 中是明文，但接口、日志、页面三处不得出现真值。
- 合法配置热加载成功，非法配置不影响旧版本运行。
- 在线状态和控制结果与真实进程一致，核心测试、构建和一次完整启动验证通过。

## 需求级验收标准

1. `server/argus_single` 已由 BADelay500W 主实现完整替换，旧 Argus 业务代码删除，其他 lightning_trade 模块保持不变。
2. 主 `application.properties` 与 `session.json` 的有效数据可在数据库中结构化、安全维护，不处理其他配置文件。
3. MySQL 保存持久化配置和会话，Redis 保存已发布运行快照、版本、热加载事件、心跳和控制状态。
4. Argus 支持安全热加载：新版本完整校验后原子切换，失败继续运行旧版本。
5. 会话数据可由 Argus 自动回写，重启后能恢复使用；API、日志和页面不泄露敏感明文（明文只在数据库，靠数据库账号权限与网络隔离防护）。
6. 管理端可维护并发布配置，查看在线状态和当前版本，并安全执行启动、停止、重启和主动热加载。
7. 历史主配置和会话可受控导入，核心交易、监控、风控和通知行为与 BADelay 主线一致。

## 已确认事项

1. `lightning_trade/server/argus_single` 当前实现全部不需要，由 BADelay500W 版本完整替换；其他 lightning_trade 代码保留。
2. 配置必须支持热加载，可使用 Redis 保存运行快照、版本和变更通知。
3. 首版只处理 `application.properties`，不处理 `application_1.properties`、`application_2.properties` 和 `dev.properties`。
4. `session.json` 数据需要入库，并支持 Argus 自动回写。
5. 管理端需要展示 Argus 状态，并提供启动、停止、重启和热加载操作。

## 后续实施决策（不阻塞本次确认）

以下事项由对应任务在需求梳理或实施阶段结合部署环境确认，不再作为本次任务拆解的前置问题：

1. **部署拓扑**：任务 5 暂按 Manager API 与 Argus 同机、调用受限启停脚本设计；若实际分开部署，则改为独立控制代理或服务管理接口。
2. **加密主密钥**：~~优先复用届时已有的 KMS/密钥服务；如果没有，则采用部署环境变量提供主密钥，数据库与 Redis 仅保存密文。~~ **已作废** —— 取消应用层加密，不再需要主密钥，见 §决策记录。
3. **最小引导配置**：默认本地保留 MySQL、Redis 连接和环境标识；`server.port`、`request.path`、`log.dir` 进入数据库，并作为修改后需要重启的参数处理。

## 待确认问题

- 无。本需求业务范围与任务拆解已确认，以上技术细节不阻塞任务创建。

## 决策记录：取消应用层加密（2026-09-08）

**决定**：`argus_config` / `argus_account` / `argus_notification` / `argus_runtime_session`
四张表的 16 个敏感列改为**明文存储**，删除 `EncryptedString` 类型与
`ARGUS_CONFIG_ENCRYPTION_KEY` 环境变量。

**原因**：运维需要直接在数据库里手工修改这些值（改一个账户的 API Key、
替换一份过期 session、调 Telegram Token）。加密后每次改都要走管理端接口或
写一次性脚本，实际维护成本高于它挡住的风险。

**为什么这个取舍站得住**：明文入库不是新增暴露面。`server/argus_single/configs/`
下的 `application.properties` / `application_1` / `application_2` 本来就在部署机
磁盘上明文存着同一批凭证（`server/argus_single/.gitignore` 的 `configs/*`
规则就是为这个），Argus 进程也必须拿到明文才能调交易所接口。加密只保护
「拿到库、但拿不到部署机」这一种情形。

**落地时的实施时机**：决策时四张表在生产库里全部 0 行、`enc:v1:` 密文 0 条，
所以不需要任何数据迁移，也不存在存量密文解不开的问题。

**必须保留的补偿手段**（删加密时不要连带删掉）：
1. `service.go` 的 `maskSecret()`：所有查询接口把敏感值掩成 `******`，
   前端与日志都拿不到真值；
2. `service.go` 的 `"******"` 哨兵：前端回填时提交 `******` 表示「此项未改」，
   服务端据此跳过覆盖。删了它会把密钥写成字面量 `******`；
3. `secretLength()`：只回长度不回内容，供页面显示「950 字符」。

**同时收紧**：数据库账号按最小权限授予，RDS 白名单只放行部署机与跳板机。

## 决策记录：配置不经 Redis 缓存，改为 DB 权威 + 定时指纹比对（2026-09-09）

**决定**：
1. 删除 Redis 里的配置快照与配置版本号（`argus:config:<key>:snapshot`、`:snapshot:<version>`、`:version`）。
   配置的读写路径只有数据库。
2. Argus 实例改为**定时（默认 60 秒）从数据库重新派生运行配置**，与当前生效配置比对
   **内容指纹**（`sha256` 于派生后的运行配置），不同则原子切换。
3. `argus:config:changed` 与 `argus:control` 保留，但角色降级为「提前触发一次比对」的
   加速信号，不再携带正确性责任。控制通道（start/stop/reload）本身继续使用。

**为什么去掉缓存**：它保护的是一天只发生数次的读取（启动 + 重载），却引入了一种静默
失效——「改了库、没清缓存」时实例继续跑旧配置，而日志照样打「配置已热加载」。
2026-09-08 的一次真实故障正是这个形状：会话凭证在库里已更新，实例读到的却是缓存里的
旧快照，盘口信号开仓全部失败，现场只看得到「获取 Web 用户凭证失败」，看不到根因。
另一个隐患是写入方有两个：管理端发布会写快照，`persistSession`（实例无头登录后回写
会话）也会写快照，两者共享同一份缓存，谁后写谁赢。

**为什么必须用内容指纹而不是版本号**：
- 运维直接改库（换 cookie/token 是每周操作）不会改动 `version`，只比版本号检测不到，
  「定时轮询」这个机制就形同虚设；
- 回滚场景下同一版本号会被重新发布，版本号相同而内容不同（这条 r7 的设计文档
  §3「生效判定只看版本号」已经指出过）。

**为什么指纹要对派生后的运行配置求取**：数据库原始行含 `updated_time` /
`created_time` / `session_updated_at` 等审计列，而 Argus 自己的会话回写就会改动它们。
对原始行求指纹 → 每次回写会话都判定「配置变了」→ `trade.ReplaceManager` +
`monitor.ReloadMonitor` 把正在扛仓的交易管理器与监控器停掉重建。这是比参数不生效
危险得多的失效方式（r5 的设计文档已论证：停进程后持仓仍挂在交易所，而移动止盈、
兜底止损、平仓监控全部失效）。派生后的运行配置只含影响行为的字段，含
cookie/token（所以凭证轮换能被检测到），不含审计列。

**代价与取舍**：
- 管理端发布的生效延迟从「秒级」变为「秒级（通知到达）或最多 60 秒（通知丢失）」。
  可接受：两条路都以数据库为准，不存在「发了通知但缓存是旧的」这种中间状态。
- 每实例每 60 秒一次快照派生（约 7 条查询）。按三实例算约 21 次/分钟，对 RDS 可忽略。
  若将来成为负担，可先比对各配置表的 `MAX(updated_time)`，变化了再做完整派生。

**运维收益**：每周轮换 cookie/token 从「改库 + 清 Redis 快照 + 发通知 + 重启」
简化为一条 UPDATE，最多 60 秒自动生效。
